# 2026-03-13

- 用户明确收敛了框架方向: 目标是基于 Koishi 机器人的类 OpenClaw workflow 架构，`workflow` 应由框架实现，`OpenClaw` 只作为普通 AI provider / adapter。
- 用户希望能力以高切面、高扩展性、高可插拔、注册即用的方式接入，重点覆盖 IIROSE 接口、点歌插件、表情包插件，以及后续可能的文字游戏插件。
- 已完成针对当前链路的审计分析，结论是现有实现仍将 `OpenClaw` 作为 workflow planner，尚未达到 provider-neutral 的框架边界。
- 已撰写细化方案文档 `docs/v0.2.0/IROSECLAW_WORKFLOW_FRAMEWORK_REFACTOR_PLAN.md`，并同步到 `/root/docs/v0.2.0/IROSECLAW_WORKFLOW_FRAMEWORK_REFACTOR_PLAN.md`。
- 文档内容已细化到目标架构、接口边界、目录重构建议、插件注册模型、迁移阶段、PR 切分、测试策略、验收标准和风险控制。
- 已继续撰写实施清单文档 `docs/v0.2.0/IROSECLAW_WORKFLOW_FRAMEWORK_IMPLEMENTATION_BACKLOG.md`，并同步到 `/root/docs/v0.2.0/IROSECLAW_WORKFLOW_FRAMEWORK_IMPLEMENTATION_BACKLOG.md`。
- 新 backlog 已细化到里程碑 M1-M6、文件级改造清单、每阶段测试矩阵、提交顺序、稳定点要求和停机点。
- 已开始落地 M1：新增 `BaseWorkflowPlanner`、`BaseModelProvider`、`LegacyOpenClawPlanner`，`WorkflowRuntime` 改为 planner-first，同时保留 orchestrator 兼容入口。
- `src/index.js` 现已支持通过 `workflow.planner` 或 `workflow.plannerFactory` 注入 planner，默认走 `LegacyOpenClawPlanner`；事件触发集成测试已改为注入 planner，而不是 patch `OpenClawAdapter.prototype.processWorkflowStep`。
- 已新增 `tests/legacy-openclaw-planner-test.js`，并把 `workflow-runtime`、`plugin-host-runtime`、`hybrid-message-handler` 等测试切到 `planner` 路径。
- 已执行 `node tests/load-test.js`、`node tests/legacy-openclaw-planner-test.js`、`node tests/workflow-runtime-test.js`、`node tests/plugin-host-runtime-test.js`、`node tests/event-trigger-integration-test.js`、`node tests/hybrid-message-handler-test.js` 与 `node tests/run.js`，均通过。
- 已继续落地 M2：新增 `src/ai/providers/openclaw-provider.js`，把 OpenClaw CLI 探测、命令构造、执行与 stdout/stderr 归一化从 adapter 中抽离为 provider。
- `src/adapters/openclaw-adapter.js` 现改为 legacy 兼容层，保留 prompt/context/legacy workflow 语义，但底层 transport 已委托给 `OpenClawProvider`。
- 已新增 `tests/openclaw-provider-test.js`，并修复 provider 对“日志前缀 + JSON payload”输出的兼容解析，保证 `conversation-context-test` 回归通过。
- 已再次执行 `node tests/openclaw-provider-test.js`、`node tests/openclaw-adapter-invocation-test.js`、`node tests/conversation-context-test.js` 与 `node tests/run.js`，均通过。
- 已继续落地 M3：新增 `MockProvider`、`LlmWorkflowPlanner`、workflow prompt compiler、decision parser/validator，并让 `src/index.js` 支持按 `workflow.planner` / `workflow.provider` 选择 planner/provider。
- `src/config/runtime.js` 现已增加默认 `workflow.planner = legacy-openclaw` 与 `providers.default = openclaw`；默认行为保持兼容，但 `llm-default` 路径已成为正式可运行路径。
- `src/adapters/openclaw-adapter.js` 的 workflow prompt 与 decision 解析已开始复用 framework 层的 compiler/parser，不再独占定义这套协议。
- 已新增 `tests/workflow-decision-parser-test.js`、`tests/llm-workflow-planner-test.js`、`tests/llm-planner-selection-test.js`，并修复 parser 对“日志前缀 + JSON payload”文本的兼容提取。
- 已执行新增测试与 `node tests/run.js`，全部通过。
- 已继续落地 M4 的第一块：`WorkflowRuntime.run()` 现已在顶层返回 `outputResults`，修复上层对“工具输出是否已发出”的误判风险。
- 已新增 `src/tools/packages/normalize.js` 与 `src/tools/packages/index.js`，并在 `PluginHost` 中实现 `registerToolPackage()`、`listToolPackages()`，同时补充了 `registerProvider()`、`registerPlanner()`、`registerHook()` 的注册面。
- `PluginHost` 的插件上下文现已暴露 `registerToolPackage/registerProvider/registerPlanner/registerHook` 等方法，package 可一次注册 tools、output plugins、trigger templates、policies、hooks。
- 已新增 `tests/tool-package-registration-test.js`，并更新 `workflow-runtime` 与 `plugin-host-runtime` 回归测试覆盖 `outputResults` 顶层返回。
- 已执行定向测试与 `node tests/run.js`，全部通过。
- 已继续把真实内建能力迁到 package 注册：`builtin-music`、`iirose-system-tools`、`iirose-room-tools`、`iirose-user-profile-tools` 现都通过 `registerToolPackage()` 接入。
- 已新增 `tests/builtin-tool-package-migration-test.js`，并更新 `tests/iirose-native-tool-groups-test.js` 断言原生 IIROSE 插件会注册对应 package。
- 已再次执行相关定向测试与 `node tests/run.js`，全部通过。
- 已继续把更多 builtin runtime 插件迁到 package 注册：`builtin-help`、`builtin-messaging-tools`、`builtin-runtime-governance`、`builtin-default-trigger-templates` 现也通过 `registerToolPackage()` 接入，分别覆盖 tools、policies、triggerTemplates 三类内容。
- `PluginHost.registerToolPackage()` 现已会为 package 内注册的 policy cleanup 挂接插件清理逻辑，避免比旧路径更差。
- 已更新 `tests/builtin-tool-package-migration-test.js` 覆盖 help/messaging/runtime-governance/default-trigger-templates package，以及相关 trigger template / policy 生效断言；再次执行 `node tests/run.js`，全部通过。
- 已开始落地 M5：新增 `src/runtime/state/store.js`、`src/runtime/state/memory-store.js`、`src/runtime/workflow/hooks/registry.js`，引入 `MemoryStateStore` 与 `WorkflowHookRegistry`。
- `WorkflowRuntime` 现已支持加载/persist 四类状态快照（workflow/session/room/user）、应用 `decision.statePatch` 与 `toolResult.statePatch`、执行 tool 自带 `outputs`，并在 `beforePlan/afterPlan/beforeToolCall/afterToolCall/beforeOutput/afterOutput/onWorkflowFinish` 这些点触发 hooks。
- `PluginHost` 现已持有 `stateStore` 与 `hookRegistry`，插件上下文可直接访问；`src/index.js` 默认装配 `MemoryStateStore` 和 `WorkflowHookRegistry`。
- `createToolResult()` 与 workflow/tool contracts 已补充 `data/outputs/statePatch` 字段；为兼容旧工具，runtime 仍保留“直接返回字符串”路径。
- 已新增 `tests/state-store-test.js` 与 `tests/workflow-hooks-test.js`，并再次执行 `node tests/run.js`，全部通过。
- 已把真实能力接入新的 `outputs + statePatch` 路径：`music.play_netease` 现通过 `playSongInSessionDetailed()` 返回结构化结果，并在成功时写入 room/user 状态。
- 音乐工具现会通过 `ToolResult.outputs` 发送“已开始播放《歌名》”类确认文本，并通过 `statePatch` 持久化 `room.currentSong` 与 `user.lastPlayedSong`。
- 已新增 `tests/music-tool-state-test.js` 验证音乐工具的输出和状态落库；再次执行 `node tests/run.js`，全部通过。
- 已开始推进 M6：`src/config/runtime.js` 的默认 `workflow.planner` 已从 `legacy-openclaw` 切换为 `llm-default`，workflow 模式默认改走 framework-owned planner。
- 已新增 `tests/workflow-default-llm-mode-test.js`，验证在未显式指定 planner 时，workflow 聊天路径会默认使用 `llm-default + providerFactory`，并且成功发送最终回复。
- 在默认 planner 切换后再次执行 `node tests/run.js`，整套回归仍通过；显式 `plannerFactory` 与 direct tool 路径未受影响。
- 已继续收紧 legacy 使用面：默认 `workflow + llm-default` 与自定义 `plannerFactory` 场景下，不再提前实例化 `OpenClawAdapter` 和 legacy `createMessageHandler`；二者改为按需惰性创建。
- `tests/workflow-default-llm-mode-test.js` 与 `tests/event-trigger-integration-test.js` 已补充 `app.adapter === null` 断言，证明默认 workflow 主链和自定义 plannerFactory 路径都不会主动拉起 legacy adapter。
- 再次执行 `node tests/run.js`，整套回归通过。
- 已进一步把 provider/planner 本身收进插件式装配：新增 `src/runtime/plugins/builtins/openclaw-provider.js` 与 `src/runtime/plugins/builtins/workflow-planners.js`，通过 `PluginHost` 注册默认 provider / planner。
- `legacy-openclaw` 现已从默认 workflow planner 插件中拆出，改为单独的 `src/runtime/plugins/builtins/legacy-openclaw-compat.js` compatibility plugin，仅在明确需要 legacy adapter 时装载。
- `PluginHost.registerProvider()` / `registerPlanner()` 现已支持 factory function，`src/index.js` 会先从 host 解析 provider/planner，再回退到内置默认实现。
- 已新增 `tests/provider-planner-plugin-registration-test.js` 验证 provider/planner 插件注册和 factory 解析；再次执行 `node tests/run.js`，全部通过。
- 已继续把 `SkillManager`、legacy skill bridge 与帮助文案从默认 workflow 主路径里下沉：默认 `workflow + llm-default` 下不再初始化 `SkillManager`，help 文案改为 runtime-first，展示能力包与工具而不是“暂无可用技能”。
- 已将 `src/index.js` 里的 legacy 组件改为按需加载，compatibility exports 也改为 getter，避免默认 workflow 主路径顶层静态加载 `OpenClawAdapter/SkillManager/createMessageHandler/LegacyOpenClawPlanner`。
- 已将默认 `runtime.mode` 与 `config/app.example.json` 都切为 `workflow`，未知 planner 会 fallback 到 `llm-default` 而不是 legacy。
- 用户要求下线 bot，已停止 `tmux` 会话 `iroseclaw`；后续开发不再有在线 bot 进程干扰。
