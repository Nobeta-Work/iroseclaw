# 2026-03-16

- 已审查项目中的飞行棋代码，并输出 `docs/飞行棋代码审查与后续开发报告.md`。
- 结论：飞行棋当前是仓库内半成品，不是已接入主链的可用功能。
- 关键问题：
  - `src/index.js` 未注册飞行棋插件，运行时实际不可用。
  - quick input 监听器会处理状态，但不会通过 `outputRuntime` 回消息。
  - 掷出 `6` 且需选飞机时，发送 `1-4` 会被误判成再次掷骰。
  - `quitTool()` 未校验参与者身份，旁观者也能结束整局。
  - 配置读取基本都写死在 `DEFAULT_CONFIG`，文档中的插件配置当前不可靠。
  - `tests/run.js` 未纳入飞行棋测试，现有 `tests/flyingchess-plugin-test.js` 也只是常量级伪测试。
- 已用临时 `PluginHost` 脚本验证以上关键问题属实。
- 之后已完成飞行棋重构：
  - `src/runtime/plugins/games/flyingchess.js` 已改为 `createFlyingChessService()` 形态，分离 service/store/tool/listener。
  - 已补上主入口注册，`src/index.js` 现在会加载飞行棋插件。
  - quick input 已接入 `outputRuntime`，房间内 `加入 / 开始 / 掷骰 / 选飞机` 会正常回消息。
  - 已修复 `6` 后选飞机状态机，不会再把 `1-4` 误判成再次掷骰。
  - 已修复“选中飞机不起作用”的问题；现在会按指定飞机编号起飞/移动。
  - 已新增 `games.flyingchess.begin` 开局工具，房间满员自动开始，未满员时参与者可显式 `开始`。
  - 已补上权限边界，旁观者不能 `quit/restart` 当前对局。
  - 已重写 `tests/flyingchess-plugin-test.js` 为真实集成回归，并纳入 `tests/run.js`。
  - 已同步更新 `docs/flyingchess.md`。
- 已执行验证：
  - `node tests/flyingchess-plugin-test.js`
  - `node tests/tictactoe-plugin-test.js`
  - `node tests/run.js`
  - 以上均通过。
- 已继续把飞行棋渲染升级为纯文本 Text UI：
  - 默认 `renderStyle=text-ui`，输出现在包含伪棋盘航线图、中心状态区、玩家状态面板和图例。
  - 新增 `renderStyle=compact` 回退模式，保留高可插拔。
  - `tests/flyingchess-plugin-test.js` 已补充 Text UI 和 compact render 覆盖。
  - `docs/flyingchess.md` 已同步更新为 Text UI 说明。
- 修改完成后已再次重启在线 `tmux` 会话 `iroseclaw`，新进程于 2026-03-16 00:59 CST 左右加载 `FLYINGCHESS` 插件并重新登录成功。
- 后续又对 Text UI 做了第二轮复审和重构：
  - 放弃“大框伪棋盘”默认视图，改为默认 `turn-card` 行动卡片，优先突出“当前/阶段/操作”。
  - `状态` / `棋盘` 现在走 `board` 详细视图，显示四段轨道、终点跑道摘要和玩家位置列表。
  - `renderStyle=text-ui` 作为兼容别名保留，但现在映射到 `board`。
  - 文档 `docs/flyingchess.md` 已同步更新为 `turn-card + board + compact` 三种渲染说明。
  - 再次执行 `node tests/flyingchess-plugin-test.js` 和 `node tests/run.js`，通过。
  - 在线 bot 已再次重启，新进程于 2026-03-16 01:14 CST 左右加载 `FLYINGCHESS` 插件并重新登录成功。
- 用户随后取消飞行棋开发计划。
- 已删除飞行棋插件源码 `src/runtime/plugins/games/flyingchess.js`、相关测试与文档，并从 `src/index.js` / `tests/run.js` 移除接入。
- 已基于当前井字棋插件输出 `docs/五子棋插件可行性分析.md`。
- 当前结论：
  - 五子棋插件在现有 runtime/plugin/quick-input 架构下可行。
  - 首版最合理范围是“双人 PVP + 纯文本棋盘 + 坐标输入 + 自由连五”。
  - 强 AI 和禁手规则不建议首版做。
- 已继续按“**双人自由连五 + 字母数字坐标 + 每步全盘**”范围实现五子棋插件：
  - 新增 `src/runtime/plugins/games/gomoku.js`，采用与井字棋一致的 `service + tools + quick input listener` 结构。
  - 当前工具为 `games.gomoku.start/join/move/status/restart/quit`。
  - 输入支持 `H8` / `8H` / `下 H8` / `落子 H8`。
  - 输出为 15x15 全盘文本棋盘，使用 `●/○/◆/◇` 表示黑白与最近一步。
  - 已在 `src/index.js` 接入，并把 `tests/gomoku-plugin-test.js` 纳入 `tests/run.js`。
  - 已补充文档 `docs/五子棋插件使用指南.md` 和 `docs/五子棋双人自由连五可行性分析.md`。
- 已执行验证：
  - `node tests/gomoku-plugin-test.js`
  - `node tests/run.js`
  - 以上均通过。
- 已按用户反馈调整五子棋棋盘渲染：
  - 空位符号从 `.` 改为 `⊡`
  - 黑白棋子使用 `⚫ / ⚪`
  - 最近一步不再在盘面上做特殊棋子标记，仅通过“最近一步”文字说明展示
  - 行号保持两位宽度显示，避免 `1` / `10` 之类错位
- 后续又修正了列标与棋盘格的对齐方式：
  - 列头 `A-O` 现在按固定两字符单元渲染，和 `⊡ / ⚫ / ⚪` 的格宽保持一致
  - 已更新 `tests/gomoku-plugin-test.js` 断言并再次执行 `node tests/gomoku-plugin-test.js`、`node tests/run.js`，通过。
- 已新增猜数字插件 `src/runtime/plugins/games/number-guess.js`：
  - 触发词 `猜数字` 开局，支持难度/位数/次数配置（如 `猜数字 困难 5位 10次`）
  - 采用 room-scoped quick input，房间内任意用户可直接发送数字参与猜测
  - 机器人按 `xAyB` 反馈，支持 `状态 / 重开 / 退出`
  - 默认无重复数字，发起者管理重开/结束，可通过配置调整
- 已把“引用回复”做成输出层通用能力：
  - `reply.current` 新增可选元数据 `metadata.quoteMessageId`
  - `src/index.js` sender 发现该字段时会构造 `h.quote(id) + h.text(text)` 发送
  - 不再是游戏插件私有逻辑，后续插件可复用
- 已新增回归 `tests/number-guess-plugin-test.js`，并纳入 `tests/run.js`
- 已执行验证：
  - `node tests/number-guess-plugin-test.js`
  - `node tests/tictactoe-plugin-test.js`
  - `node tests/plugin-host-runtime-test.js`
  - `node tests/run.js`
  - 全部通过
- 已新增文档 `docs/猜数字插件可行性分析.md`，说明 IIROSE 引用回复可行性与扩展边界。
- 已复查猜数字链路并补全“规则触发”能力：
  - `src/runtime/plugins/games/number-guess.js` 新增 `games.number-guess.rules`
  - 支持 `猜数字 规则 / 猜数字规则 / 数字游戏 规则 / ab游戏 规则` 直达规则说明
  - `src/runtime/plugins/games/tictactoe.js` 新增 `games.tictactoe.rules`
  - 支持 `井字棋 规则 / 井字棋规则 / ttt 规则` 直达规则说明
  - 两个插件的 triggerTemplates 已同步纳入 `*.rules` 工具
- 已补充回归断言：
  - `tests/number-guess-plugin-test.js` 增加 rules 工具注册、返回内容、`matchMessage('猜数字 规则')` 命中验证
  - `tests/tictactoe-plugin-test.js` 增加 rules 工具注册、返回内容、`matchMessage('井字棋 规则')` 命中验证
- 已执行验证：
  - `node tests/tictactoe-plugin-test.js`
  - `node tests/number-guess-plugin-test.js`
  - `node tests/run.js`
  - 全部通过
- 已按新需求再次调整：
  - `猜数字` 的位数设置改为“程序随机决定”，不再由用户输入直接指定
  - 难度预设升级为位数区间（`digitsRange`），开局时按难度区间随机位数
  - `猜数字规则` 文本改为“完整规则说明”，包含各难度档位区间与次数，不再只描述当前局状态
  - `井字棋规则` 文本补充完整玩法说明（单人/双人/编号映射/控制命令）
- 已同步更新测试断言并复测：
  - `node tests/number-guess-plugin-test.js`
  - `node tests/tictactoe-plugin-test.js`
  - `node tests/run.js`
  - 全部通过
- 已继续按新规则收口猜数字：
  - 各难度改为固定数位：`easy=3`、`normal=4`、`hard=5`、`expert=6`（取原低位）
  - 每局基础猜测次数统一固定为 `10`，不再接受用户输入覆盖
  - 新增失败后续命机制：发送 `延长续命十次`（或 `续命十次`）可在失败局上 +10 次并继续
  - 默认允许重复数字（`allowRepeatDigits=true`）
  - 已新增 `games.number-guess.extend` 工具并接入触发模板
- 已再次执行验证：
  - `node tests/number-guess-plugin-test.js`
  - `node tests/tictactoe-plugin-test.js`
  - `node tests/run.js`
  - 全部通过
- 已新增“模式切换”能力：
  - 支持 `猜数字 模式` 查询当前是否允许重复数字
  - 支持 `猜数字 模式 可重复` / `猜数字 模式 不重复` 切换
  - 新增工具 `games.number-guess.mode` 并纳入 trigger templates
  - 模式为房间级设置；正在进行中的对局保持不变，新模式从下一局生效
- 已补充 `tests/number-guess-plugin-test.js` 覆盖模式命令路由与切换后生效行为，并再次通过整套回归。
- 最终又按用户要求回退到“上方字母、左侧数字”的棋盘：
  - 输入主格式为 `数字+字母`，如 `8H`；`H8` 仍兼容并会规范化为 `8H`
  - 棋盘恢复为 `01-15` 行号 + 顶部 `A-O` 列头
  - 顶部列头现改为 emoji 风格 `🇦-🇴`，并保留固定前导空格与可调间距
  - 空位仍为 `⊡`，黑白子仍为 `⚫ / ⚪`
  - 已再次执行 `node tests/gomoku-plugin-test.js` 和 `node tests/run.js`，通过。
- 在线 bot 已再次重启，新进程于 2026-03-16 18:25 CST 左右加载 `GOMOKU` 插件并重新登录成功。
- 已完成“日志/记忆收敛”改造：
  - `src/plugins/message-memory.js` 新增落盘压实机制：按 `maxEventsPerChannel` 保留最近窗口，支持 `compactCheckInterval` 周期压实与 `compactOnStartup` 启动压实，避免 `data/message-memory/*.jsonl` 无上限增长。
  - `@` 记忆新增硬上限：`maxAnchorRounds=20`，超出轮数会压缩为摘要并暴露 `anchorCountRetained`。
  - `src/config/runtime.js` 默认配置新增 `messageMemory.maxAnchorRounds / compactCheckInterval / compactOnStartup`，并加入 normalize 逻辑。
  - `config/app.example.json` 已补齐 `messageMemory` 示例配置块。
  - 新增回归 `tests/message-memory-retention-test.js`（覆盖落盘保留与 20 轮 anchor 收敛），并纳入 `tests/run.js`。
  - 已执行 `node tests/message-memory-retention-test.js`、`node tests/context-service-test.js`、`node tests/run.js`，全部通过。
- 已按“不要按天日志、避免无限增长”继续重构审计日志：
  - `src/runtime/audit/workflow-run-log.js` 改为固定单文件 `workflow-runs.jsonl`（可配置），新增 `maxBytes/targetBytesAfterCompact/compactCheckInterval` 容量压实策略；超限后仅保留最近窗口并原子替换写回。
  - `src/core/audit.js` 改为固定单文件 `logs/audit/audit.log.jsonl`（可通过环境变量覆盖文件名和容量阈值），同样支持容量压实与原子替换，不再按日期分文件。
  - `src/config/runtime.js` 与 `config/app.example.json` 已新增 `workflowRunLog.fileName/maxBytes/targetBytesAfterCompact/compactCheckInterval` 配置。
  - `tests/workflow-run-log-test.js` 已更新：验证单文件命名与超限压实后保留最新记录。
  - 已执行 `node tests/workflow-run-log-test.js` 与整套 `node tests/run.js`，全部通过。
- 已执行数据迁移（单文件日志策略落地后的历史数据收口）：
  - `data/workflow-runs/2026-03-11..16.jsonl` 已合并进 `data/workflow-runs/workflow-runs.jsonl`（397 条）并移动到备份目录。
  - `logs/audit/2026-03-10..16.jsonl` 已合并进 `logs/audit/audit.log.jsonl`（640 条）并移动到备份目录。
  - `data/message-memory/*.jsonl` 已按每文件最多 400 条压缩（3 个文件被压缩，整体从 22695 行降到 1237 行）。
  - 迁移备份目录：`data/migration-backup/log-migration-2026-03-16T15-16-52-294Z`（包含旧分片日志与压缩前 message-memory 副本，可用于回滚）。
- 已完成 OpenClaw Adapter 稳定性修复与接口收敛：
  - `src/ai/providers/openclaw-provider.js` 新增公共方法 `resolveInvocation/buildExecArgs/extractTextFromJson/extractTextFromPlain/formatErrorReason`，用于替代 adapter 对 provider 私有 `_xxx` 方法的强耦合调用。
  - `src/adapters/openclaw-adapter.js` 增强：
    - workflow provider 失败不再伪装为 `final`，改为 `status=error`；
    - 识别 provider 错误文案（HTTP/timeout/config/ENOENT 等）并返回 `error`；
    - 新增轻量重试与退避（`retry.maxRetries/retry.retryDelayMs`）；
    - session queue key 改为房间/会话稳定 key（不再依赖 `Date.now()` 回退）；
    - 统一使用 `logger` 记录 retry/error。
  - 新增回归 `tests/openclaw-adapter-stability-test.js`，覆盖失败语义、错误文案判定、fallback 和队列 key 稳定性；并纳入 `tests/run.js`。
  - 已执行 `node tests/openclaw-provider-test.js`、`node tests/openclaw-adapter-stability-test.js`、`node tests/openclaw-adapter-invocation-test.js`、`node tests/legacy-openclaw-planner-test.js`、`node tests/run.js`，全部通过。
- 已按“希望更积极发言”完成主动介入模块调参：
  - `src/runtime/plugins/proactive/topic-engagement.js` 默认阈值下调为更积极：
    - `minMessages: 6 -> 4`
    - `minParticipants: 3 -> 2`
    - `maxAverageGapMs: 12000 -> 18000`
    - `maxSpeakerRatio: 0.6 -> 0.75`
    - `minBotSilenceMs: 180000 -> 45000`
    - `cooldownMs: 8min -> 3min`
  - 新增按话题原因的触发加权：
    - `question`：更早介入（消息阈值-1，节奏阈值放宽）
    - `tension`：更积极介入（消息阈值-2，参与者阈值-1，节奏/发言占比阈值放宽）
  - 已执行 `node tests/proactive-topic-engagement-test.js` 与整套 `node tests/run.js`，全部通过。
