# 2026-03-14

- 已继续基于“最终构想优先”收口架构：默认主路径保持 `workflow + llm-default + plugin-registered provider/planner`。
- 旧框架文档中的关键偏差已修正：`docs/IROSECLAW_FRAMEWORK_GUIDANCE.md` 与 `docs/IROSECLAW_FRAMEWORK_DEVELOPMENT_DRAFT.md` 不再把 OpenClaw 写成 workflow 决策引擎，也不再把 `legacy` 作为默认推荐模式。
- `src/index.js` 里的 legacy 组件继续下沉：`OpenClawAdapter`、`SkillManager`、`createMessageHandler`、`LegacyOpenClawPlanner`、`OpenClawWorkflowOrchestrator` 现都改为按需加载或 compatibility getter。
- 默认 workflow 主路径下不再初始化 `SkillManager`，帮助文案也已经切成 runtime-first，展示能力包与工具，而不是围绕 skills。
- 默认运行配置与示例配置都已切为 `runtime.mode = workflow`。
- 未知 planner 名称现在会 fallback 到 `llm-default`，不再 silently 回退到 legacy planner。
- bot 已按用户要求下线，`tmux` 会话 `iroseclaw` 已停止，当前没有在线 bot 干扰开发。
- 本轮结束后再次执行 `node tests/run.js`，整套回归通过。
- 已审计并修复 `remote-room-monitoring` 插件的设计缺陷：原实现既会错误覆盖 `message.private` trigger template，又依赖 planner 做总结，导致私聊“查看房间状况”无法稳定返回审查报告。
- `src/runtime/plugins/monitoring/remote-room-monitoring.js` 现已改为工具内自洽完成“解析目标房间 -> 读取上下文 -> LLM/启发式分析 -> 私聊回管理员报告”，默认优先读取 bot 当前所在房间，且不再把原始房间消息直接作为 tool result 吐回管理员。
- `TriggerTemplateRegistry` 与 trigger template normalization 已支持保留 `instruction` 并按增量方式合并模板，避免后续插件插入时把默认 `toolNames`、`allowDirectToolMatch`、`sendFallbackOnError` 覆盖掉。
- 已新增 `tests/remote-room-monitoring-test.js`，并扩展 `tests/trigger-template-test.js` 覆盖模板合并与 prompt 注入；再次执行 `node tests/run.js`，整套回归通过。
- 已完成对 `remote-room-monitoring` 的“高可插拔性”审计：当前状态可注册、可运行，但仍存在核心模板硬编码工具名、provider 解析重复实现、全局 service 名冲突、平台耦合 IIROSE、宿主缺少完整反注册/热替换等问题。
- 用户已决定将 `remote-room-monitoring` 的“高可插拔化”列为后续待开发事项，当前轮次只做记录，不继续推进实现。
- 已新增 `src/runtime/plugins/proactive/topic-engagement.js` 作为独立 runtime 插件，替代“主动消息”需求的零散能力：在群聊高频、多参与者、低 bot 近期发言的场景下，按冷却策略轻量插话。
- 新插件已提供管理员私聊控制工具：`开启主动模式`、`关闭主动模式`、`主动模式状态`、`命名主动模式 xxx`；默认实现为谨慎模式，插件初始默认关闭，需管理员显式开启。
- 主动介入插件已做成相对高插拔形态：通过 tool package + private trigger template merge + namespaced service `proactive.topic-engagement` 接入，不修改核心 workflow；同时支持 pluginConfig 级别的 `shouldIntervene` / `composeIntervention` / provider 覆盖。
- 已新增 `tests/proactive-topic-engagement-test.js` 覆盖管理员控制和高频群聊介入链路，并更新帮助页跳过 `helpVisible=false` 的 admin-only 工具；再次执行 `node tests/run.js`，整套回归通过。
- 代码变更后已重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-14 22:35 CST 左右重新登录成功。
