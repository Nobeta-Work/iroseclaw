# 2026-03-15

- 已按用户要求将项目侧 `OpenClawProvider` 收敛为默认无状态语义：`openclaw.stateless` 默认开启，provider 每次调用都会生成隔离 session，不再跨请求复用 OpenClaw agent 会话。
- `src/ai/providers/openclaw-provider.js` 现支持 `statefulSession` 显式开关；只有在明确要求依赖 OpenClaw 原生会话记忆时，才保留稳定 `sessionId`。
- `src/adapters/openclaw-adapter.js` 已改为仅在 `useNativeSessionContext=true` 时才向 provider 传递 `statefulSession=true`，避免 workflow/planner/监控/主动介入链路继续错误复用 OpenClaw session。
- `src/config/runtime.js`、`config/app.example.json`、`src/runtime/plugins/builtins/openclaw-provider.js` 与 `src/index.js` 已同步新增/透传 `openclaw.stateless` 配置，默认行为现与“OpenClaw 仅作为普通 AI provider”目标一致。
- 已补充回归测试：`tests/openclaw-provider-test.js` 验证默认 stateless 与显式 stateful 行为，`tests/conversation-context-test.js` 验证 native session context 下仍可稳定复用 session；再次执行 `node tests/run.js`，整套回归通过。
- 代码变更后已重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 00:19 CST 左右重新登录成功。
- 已新增 `src/ai/providers/openai-compatible-provider.js`，提供真正的无状态 HTTP chat-completions provider，可作为普通 AI API 后端接入 framework 主链。
- 已新增 `src/runtime/plugins/builtins/openai-compatible-providers.js`，会从 `config.providers.named` 自动注册启用的普通 API provider；`providers.default` 现可直接切到这些 named provider，而不需要再走 OpenClaw agent CLI。
- `src/config/runtime.js` 与 `config/app.example.json` 已扩展 `providers.named` 配置结构，支持 `type/baseUrl/apiKey/model/timeout/headers/maxTokens/enabled` 这类常见字段。
- 已保持 OpenClaw 兼容：当前 `OpenClawProvider` 仍可用，但 framework 已不再被迫只依赖它；后续可继续把现有 OpenClaw agent 调用路径正式下沉为 `AgentBridge`。
- 已新增 `tests/openai-compatible-provider-test.js`，并扩展 `tests/provider-planner-plugin-registration-test.js` / `tests/load-test.js`；再次执行 `node tests/run.js`，整套回归通过。
- 代码变更后再次重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 01:30 CST 左右重新登录成功。
- 已继续把当前 `openclaw` 路径语义下沉为兼容层：`src/ai/providers/openclaw-provider.js` 内部主类现已重命名为 `OpenClawAgentBridge`，`OpenClawProvider` 仅作为兼容别名保留；同时新增 `src/ai/providers/openclaw-agent-bridge.js` 作为显式导出入口。
- `src/adapters/openclaw-adapter.js` 现已明确依赖 `OpenClawAgentBridge`，`src/runtime/plugins/builtins/openclaw-provider.js` 现同时注册 `openclaw`（兼容别名）和 `openclaw-agent`（显式 bridge 名称），避免继续把 agent runtime 误解为普通 provider。
- `src/index.js` 的 provider 解析已收紧：对于 `providers.named` 中声明的普通 HTTP provider，会直接构造 `OpenAICompatibleProvider`；未知 provider 名称不再静默回退到 OpenClaw bridge，而是返回明确错误 stub。
- 已新增 `tests/named-provider-default-selection-test.js`，验证 `providers.default=analysis-http` 时 workflow 主链确实选择普通无状态 provider，而不是偷偷回退到 OpenClaw bridge；再次执行 `node tests/run.js`，整套回归通过。
- 代码变更后再次重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 01:43 CST 左右重新登录成功。
- 已直接调整 OpenClaw 子代理基准提示词 `/root/.openclaw/agents/iirose/agent/config.json`：从“活泼可爱、50 字内”改为更像真实群聊常驻成员的口语化、轻松、有生活感表达，减少客服腔和说明文腔。
- 修改后已重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 01:56 CST 左右重新登录成功，新人格提示已生效。
- 已补强 `iirose.room.move` 的自然语言意图覆盖：工具描述和别名现显式包含“转移到 / 移动到 / 移步到 / 去某房间 / 把 bot 挪到某房间”等表达，并支持 `<sharp id=\"...\"/>` 和纯房间 ID。
- `src/runtime/workflow/prompt/compiler.js` 已新增房间迁移自然语言示例，`src/runtime/plugins/iirose/room.js` 也已给 `message.mentioned` / `message.private` 注入 room-move 领域指令，帮助 planner 更稳定地将自然表达规划到 `iirose.room.move`。
- 已扩展 `tests/admin-workflow-prompt-test.js` 与 `tests/room-move-command-test.js`，覆盖 prompt 中的房间迁移示例以及 `转移到 / 移动到` 这类实际表达；再次执行 `node tests/run.js`，整套回归通过。
- 代码变更后再次重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 02:38 CST 左右重新登录成功。
- 已按文档方向实现新的上下文逻辑：`MessageMemoryStore.buildContext()` 不再只返回线性最近消息，而是同时输出“最近与 bot 相关消息”(`recentMessages`)、“当前频道最近消息”(`channelRecentMessages`) 与 `historySummary`，更接近文档里对会话上下文 / 频道上下文 / 短期 memory 的分层目标。
- 上下文结构现已保留 `rawContent`，并在标准 prompt 中按需展示 `raw=...`，避免 `<sharp id=...>`、原始 at、图片/json 标记在模型侧完全丢失；相关改动在 `src/plugins/message-memory.js`、`src/runtime/context/service.js` 与 `src/runtime/workflow/prompt/serializers.js`。
- `src/index.js` 中 workflow planner 的 `useNativeSessionContext` 现已与 provider 能力绑定：只有 provider 明确支持 stateful session（如 `OpenClawAgentBridge`）时，才会给模型注入“同一 session 的历史会自动保留”提示，避免对 stateless HTTP provider 说谎。
- 已扩展 `tests/conversation-context-test.js`、`tests/context-service-test.js` 与 `tests/llm-planner-selection-test.js`，覆盖 raw content、anchor summary、channel recent context 与 stateless provider 下禁用 native session hint；再次执行 `node tests/run.js`，整套回归通过。
- 代码变更后再次重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 02:59 CST 左右重新登录成功。
- 已排查“AI 回复经常首轮失败、重发才正常”的线上问题：`data/workflow-runs/2026-03-15.jsonl` 中失败样本（如 `messageId=410930959174`）统一落在 `invalid workflow decision: workflow decision payload must be an object`，随后同内容重发可正常返回，说明错误在 workflow decision 解析层而非消息发送层。
- `src/runtime/workflow/decision/parser.js` 现已增强为可解包双层字符串 JSON 与单元素数组 JSON，兼容 OpenClaw/provider 偶发返回的“字符串化 decision”脏输出，避免首轮直接掉进 `智慧已瓦解，请联系管理员` 兜底。
- 已扩展 `tests/workflow-decision-parser-test.js` 与 `tests/llm-workflow-planner-test.js`，覆盖 stringified JSON / single-item array decision 回归；先执行 `node tests/workflow-decision-parser-test.js`、`node tests/llm-workflow-planner-test.js`、`node tests/workflow-runtime-test.js`、`node tests/runtime-mode-integration-test.js`、`node tests/trigger-router-test.js`，随后再次执行整套 `node tests/run.js`，均通过。
- 修复后已重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 12:02 CST 左右重新登录成功。
- 用户复查后确认问题仍存在。继续排查发现：修复后的新失败样本（如 `data/workflow-runs/2026-03-15.jsonl` 中 `messageId=36610109619`）并不是模型返回了“数组 decision”，而是 OpenClaw 偶发把模型报错文本作为正常 payload 返回，例如 `HTTP 400: ... Range of input length should be [1, 202752]`。
- 之所以日志长期显示 `invalid workflow decision: workflow decision payload must be an object`，是因为原 parser 会从上述错误文案里把 `[1, 202752]` 误提取成 JSON 数组；这属于误诊，不是实际根因。
- 进一步用真实 `OpenClawProvider + compileWorkflowPrompt()` 多次调用复现后确认：当前 `iirose` 子代理 workspace 指向 `/opt/projects/iroseclaw`，OpenClaw 会把整个项目的 AGENTS/skills/tools 注入 system prompt；部分较小 context 模型（如 `modelscope/ZhipuAI/GLM-5`）会因输入过长直接返回 400，而更大 context 的 `bailian/qwen3.5-plus` 又能成功，因此线上表现为“首轮失败，重发才正常”。
- 已做两层代码侧兜底：`src/runtime/workflow/decision/parser.js` 不再把错误文案里的范围数组误判为 decision JSON；`src/runtime/workflow/planners/llm-workflow-planner.js` 现会对 OpenClaw 返回的这类 provider/模型错误文本自动重试一次，尽量把“人工重发一次才恢复”收敛为运行时内部重试。
- 已新增回归：`tests/workflow-decision-parser-test.js` 覆盖 `HTTP 400 ... [1, 202752]` 误判场景，`tests/llm-workflow-planner-test.js` 覆盖 OpenClaw 首次返回 provider 错误文本、第二次成功时的自动重试；再次执行 `node tests/run.js`，整套通过。
- 新修复已再次重启在线 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 12:20 CST 左右重新登录成功。
- 已阅读当前 runtime/plugin/iirose adapter 链路，并输出 `docs/井字棋插件可行性分析报告.md`：确认“无需 LLM 的井字棋插件”可直接走 `message.mentioned + direct-tool` 实现，但当前 `koishi-plugin-adapter-iirose` 未发现按钮/点击回调协议，第一版应采用文本棋盘 + `@Bot 井字棋 5` / `@Bot 下5` 这类前缀落子方案，而不应先追求 9 格真交互面板。
- 已进一步排查用户提到的“艾薇消息里数字编号可点后弹出小窗选择”能力：当前本地实例数据只有 `data/adapter-iirose/<uid>/wsdata/userlist.json` 与 `roomlist.json`，没有普通消息原始日志，也未搜到“艾薇”样本；adapter 源码显示普通消息只解码为 text/image/audio/video/at/sharp/link/markdown/quote，事件侧也无 click/callback 类型，因此现阶段无法证明这是 bot 可用的原生交互协议。更可能是客户端本地弹窗、未支持的私有协议或第三方脚本；若要确认，下一步需抓取一条艾薇原始 WS 消息及一次点击后的回包样本。
- 已在 `data/message-memory/386251acc6259c2b1b360ea17c9e5ca837acbbed.jsonl` 找到关键样本：艾薇会发 `<at .../> 加入游戏 [.%uid.]` 这类消息，但日志显示真正稳定的触发文本仍是用户手动发送 `小艾加入%uid`；裸发 `[.%uid.]` 本身不会直接触发加入成功回复。
- 已新增 runtime 插件 `src/runtime/plugins/iirose/interaction-probe.js` 并接入 `src/index.js` 注册：现在可以通过 `@Bot 测试交互` 让 bot 回发真实 token `[.%你的UID.]`、自定义 token 与对照命令 `小艾加入%你的UID`，同时插件会被动记录 `艾薇` / `[.%...]` / `加入%...` 相关消息。
- 已新增 `@Bot 最近交互探针` 命令，用于直接在聊天里查看最近捕获到的实验回传，不必手动翻文件。
- 已新增回归测试 `tests/interaction-probe-plugin-test.js`，并更新 `tests/run.js`；已执行 `node tests/interaction-probe-plugin-test.js` 与 `node tests/load-test.js`，通过。
- 已检查 `Noβ` 的首次交互探针实验：`data/message-memory/386251acc6259c2b1b360ea17c9e5ca837acbbed.jsonl` 中 `17421/17422` 确认 `测试交互` 与 bot 探针消息已发送，但之后未出现 `[.%69ad815e0da8c.]`、`[.%probe_e0da8c.]` 或 `小艾加入%69ad815e0da8c` 相关回传；`data/interaction-probe/events.jsonl` 也未捕获 join-command / probe-token，仅记录到两条无关的艾薇普通发言，说明本轮实验尚未产生可用于证明“点击会回传消息”的证据。
- 用户已补充实测结果：对 bot 发出的探针消息中的 token 点击后，客户端表现为“纯文本”，没有弹窗填充，也没有自动生成输入文本。该结果说明仅靠发送 `[.%uid.]` 字面串，无法在当前 bot 消息上复现艾薇的交互效果；后续若继续排查，重点应转向“艾薇原始 WS 消息是否含有日志里未保留的隐藏协议/元数据”，而不是继续尝试纯文本复刻。
- 已按用户要求收口方向：放弃艾薇/隐藏点击协议路线，并输出 `docs/井字棋准交互插件方案.md`。当前结论是 IIROSE 文档和 adapter 都没有公开点击式交互 API；推荐改为“准交互”井字棋插件：显式命令走 direct-tool，游戏进行中通过插件自己的 `ctx.on('message')` 监听器接管当前棋手在当前房间发送的纯数字 `1-9`、`重开`、`退出` 等短消息，实现近似点击的体验，同时保持高可插拔（service/tool/listener/config hook 分层）。
- 已完成井字棋准交互插件实现：新增 `src/runtime/plugins/games/tictactoe.js`，采用独立 runtime plugin 形态，提供 service + tools + room-scoped quick input listener；支持 `@Bot 井字棋` 开局，活跃对局中直接发送 `1-9` 落子，以及 `状态 / 重开 / 退出` 控制。
- 插件已接入 `src/index.js` 注册；新增回归测试 `tests/tictactoe-plugin-test.js` 并更新 `tests/run.js`。
- 已执行 `node tests/tictactoe-plugin-test.js`、`node tests/load-test.js`、`node tests/run.js`，全部通过。
- 已重启线上 bot，`tmux` 会话 `iroseclaw` 于 2026-03-15 19:58 CST 左右重新登录成功，井字棋插件已生效。
- 已按用户要求将井字棋扩展为双人制：当前 `src/runtime/plugins/games/tictactoe.js` 同时支持单人 `@Bot 井字棋` 与双人 `@Bot 井字棋 双人` / `@Bot 双人井字棋`；双人模式下第二位玩家可直接发送 `加入` / `加入井字棋` 应战，之后双方都可在轮到自己时直接发送 `1-9` 落子。
- 双人版回归已补到 `tests/tictactoe-plugin-test.js`，覆盖 waiting/join/out-of-turn/restart/quit；再次执行 `node tests/tictactoe-plugin-test.js`、`node tests/load-test.js` 与整套 `node tests/run.js`，通过。
- 为上线双人版再次重启 `tmux` 会话 `iroseclaw`；启动时出现外部 HTTP 超时堆栈，但进程仍在运行并持续收到房间消息，说明 bot 主链未挂。
