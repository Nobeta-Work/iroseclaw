# 2026-03-17

- 已按用户要求重构帮助展示策略：
  - `src/services/help/overview.js` 从“工具/能力包枚举”改为“用户导向引导”。
  - 默认隐藏内部操作型工具（尤其 sideEffect 工具、adminOnly 工具、`reply.current`/`message.route`）。
  - 帮助文案新增“你可以直接这样说”“常见示例”，并明确提示“内部操作已隐藏”。
- 为确保用户可见能力仍可展示，`src/tools/builtins/music-play-netease.js` 为 `music.play_netease` 显式标记 `metadata.helpVisible=true`（保留“点歌”在帮助中出现）。
- 新增回归 `tests/help-overview-visibility-test.js`，覆盖：
  - 用户可见指令保留（如“点歌”）
  - 操作型工具隐藏（如“切房”）
  - 管理工具隐藏（如“开启主动模式”）
  - 帮助文本包含“内部操作已隐藏”说明
- 更新 `tests/runtime-mode-integration-test.js` 断言，使其匹配新帮助目标（用户引导而非 runtime 包明细）。
- 已执行验证：
  - `node tests/help-overview-visibility-test.js`
  - `node tests/runtime-mode-integration-test.js`
  - `node tests/run.js`
  - 全部通过。
- 已继续实现“帮助双版本”：
  - 普通用户版：保持用户导向引导，隐藏管理/运维内部操作。
  - 管理员版：在不暴露内部执行细节的前提下，新增“管理员快捷指令”区块（从 `adminOnly` 工具的 `directAliases` 自动提取）。
  - `help.show` 现在会基于调用者 UID 与 `config.admins` 判断是否管理员并渲染对应版本。
- 主要改动：
  - `src/tools/builtins/help-overview.js` 引入 `isAdminUser`，`execute(context)` 按调用者身份传入 `isAdmin`。
  - `src/runtime/plugins/builtins/help.js` 在构建 help tool 时注入 `runtimeConfig`。
  - `src/services/help/overview.js` 新增管理员版区块与文案分支。
  - `tests/help-overview-visibility-test.js` 新增管理员版断言。
- 已执行验证：
  - `node tests/help-overview-visibility-test.js`
  - `node tests/runtime-mode-integration-test.js`
  - `node tests/run.js`
  - 全部通过。
- 已完成 llm workflow 提示词“可插拔 + 可配置 + 管理员运行时切换”改造：
  - `src/runtime/workflow/prompt/compiler.js` 不再仅靠固定硬编码风格，新增 `promptProfile` 注入（机器人身份、当前风格、风格指令、额外要求）。
  - `src/config/runtime.js` 与 `config/app.example.json` 新增 `workflow.promptProfile` 配置，默认提供 `plain/warm/affectionate`（平淡/热情/爱慕）并支持自定义别名、说明与持久化状态文件。
  - 新增 `src/runtime/workflow/prompt/profile-service.js` 作为风格状态服务，支持解析别名、切换风格、持久化当前风格。
  - 新增 `src/runtime/plugins/builtins/workflow-prompt-profile.js`：
    - 管理员工具 `workflow.prompt.style.status`
    - 管理员工具 `workflow.prompt.style.set`（支持“切换风格 平淡/热情/爱慕”“平淡模式”等自然语言）
  - `src/index.js` 注册 `builtin-workflow-prompt-profile`，并在 planner 构造中注入 `promptProfileService`，使切换立即生效到 LLM prompt。
  - `src/runtime/plugins/builtins/workflow-planners.js` 与 `src/runtime/workflow/planners/llm-workflow-planner.js` 已传递并使用 prompt profile 配置/服务。
  - `src/runtime/trigger/templates.js` 把风格工具加入 `default/message.mentioned/message.private` 可见工具集。
  - 新增回归 `tests/workflow-prompt-profile-test.js` 并纳入 `tests/run.js`。
  - 已执行 `node tests/run.js`，全量通过。
- 已按“强收敛单配置”完成配置体系收口：
  - 统一配置入口为 `config/app.json`，`src/config/runtime.js` 加载顺序改为 `DEFAULT -> app.json -> ENV`。
  - 移除旧并行配置文件：`config/app.example.json`、`config/bot.json`、`koishi.example.yml`。
  - `README.md` 已改为单配置说明，并新增“配置字段总览 + 提交前脱敏/提交后恢复”流程。
  - 新增脚本：
    - `scripts/config-extract.js`：抽离 `config/app.json` + `koishi.yml` 敏感值到 `/tmp/iroseclaw-secrets-<ts>.json`，并把仓库配置写为初始化值。
    - `scripts/config-restore.js`：从临时备份回填本机配置。
  - `package.json` 新增命令：`npm run config:extract` / `npm run config:restore -- <backup>`。
  - `.gitignore` 已移除旧 `app.local/bot/koishi.yml` 忽略项，避免单配置模式下配置文件缺失。
  - 已执行验证：
    - `node scripts/config-extract.js`
    - `node scripts/config-restore.js /tmp/iroseclaw-secrets-...json`
    - `node tests/run.js`
    - 全部通过。
