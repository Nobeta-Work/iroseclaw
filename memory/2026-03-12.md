# 2026-03-12

- 21:27 左右，IIROSE 回复异常的直接原因不是“人工智能标签”，而是 OpenClaw CLI 解析失败。
- `data/workflow-runs/2026-03-12.jsonl` 最近两条真实消息记录显示 audit.reason 为 `spawn openclaw ENOENT`，但发送链路仍然正常，机器人只是退回到兜底文案。
- `/usr/lib/node_modules/openclaw` 处于损坏状态，只剩 `node_modules/`，缺失 `package.json` 和 `dist/index.js`。
- 可用的 OpenClaw 包实际位于 `/usr/lib/node_modules/.openclaw-gX1GdeX9/`，其中 `openclaw.mjs` 与 `dist/index.js` 完整。
- 已修改 `src/adapters/openclaw-adapter.js`：优先使用显式环境变量，其次尝试 `openclaw` 命令，失败后回退到包目录入口文件。
- 已新增 `tests/openclaw-adapter-invocation-test.js` 覆盖回退逻辑，并已重启 `tmux` 会话 `iroseclaw` 使修复生效。
- 22:28 重新启动了 `tmux` 会话 `iroseclaw`，命令仍为 `cd /opt/projects/iroseclaw && npm run dev`。
- 新进程已在 22:28:55 成功登录 IIROSE，账号显示为 `Yesα`，房间 ID 为 `69ac4a8c625c1`。
- 登录后收到 `Noβ` 的 `@Yesα hello?`，日志显示已进入 `OpenClawAdapter` 并选择 `openclaw` 调用，但截至 22:30 左右尚未看到该次调用完成日志。
- 已将表情包检索关键词模板从“`情绪/表情包/楠娘/动漫`”调整为“`情绪信息 + 表情包 + 白圣女`”。
- `src/services/meme/search.js` 现已真正接入 `IROSE_MEME_STYLE_KEYWORD`，默认值改为 `白圣女`；空情绪时回退为 `情绪信息`。
- 已补充 `tests/meme-service-test.js` 覆盖默认关键词、环境变量覆盖和 query 生成结果，测试通过。
- 22:47 重启 `tmux` 会话 `iroseclaw` 后，机器人已在 22:47:30 再次成功登录 IIROSE。
